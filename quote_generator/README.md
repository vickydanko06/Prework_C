# Quote Generator

A project that will generate random quotes

## Setup

1. Clone this repo
2. Create virtual environment: `python3 -m venv venv`
3. Activate: `source venv/bin/activate`
4. Install dependencies: There are no dependencies for this project.
5. Run: `python main.py`